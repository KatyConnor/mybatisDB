import hx.data.mybatis.entity.WhereSqlEntity whereSqlEntity;
import hx.data.mybatis.param param;

interface service;
method queryByParam;
SELECT whereSqlEntity FROM user LEFT JOIN menue ON param.id = param.id WHERE param;

